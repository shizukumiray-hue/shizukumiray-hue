name: Build APK

on:
  push:
    branches: [ dev, main ]
  pull_request:
    branches: [ dev, main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build APK (Other Debug)
      run: ./gradlew assembleOtherDebug --stacktrace
      
    - name: Build APK (Other Release - unsigned)
      run: ./gradlew assembleOtherRelease --stacktrace || ./gradlew assembleOtherDebug --stacktrace
      continue-on-error: true
      
    - name: List built APKs
      run: find app/build/outputs/apk -name "*.apk" -type f
      
    - name: Upload APK artifacts
      uses: actions/upload-artifact@v4
      with:
        name: sing-box-apks
        path: app/build/outputs/apk/**/*.apk
        retention-days: 30
