name: Build APK with libbox from source

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Android app
      uses: actions/checkout@v4
      
    - name: Checkout sing-box to subdirectory
      run: |
        cd ..
        git clone --depth 1 --branch v1.14.0-rc.1 https://github.com/SagerNet/sing-box.git
      
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version: '1.23'
        
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        
    - name: Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: Install Android NDK
      run: |
        echo "y" | $ANDROID_SDK_ROOT/cmdline-tools/latest/bin/sdkmanager --install "ndk;26.1.10909125"
        echo "ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/26.1.10909125" >> $GITHUB_ENV
        
    - name: Build libbox.aar
      working-directory: ../sing-box
      run: |
        export ANDROID_HOME=$ANDROID_SDK_ROOT
        export ANDROID_NDK_HOME=$ANDROID_SDK_ROOT/ndk/26.1.10909125
        make lib_android
        
    - name: Copy libbox.aar to Android project
      run: |
        mkdir -p app/libs
        find ../sing-box -name "libbox*.aar" -exec cp {} app/libs/ \;
        ls -lh app/libs/
        
    - name: Cache Gradle packages
      uses: actions/cache@v4
      with:
        path: |
          ~/.gradle/caches
          ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/gradle-wrapper.properties') }}
        restore-keys: |
          ${{ runner.os }}-gradle-
          
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build APK (Other Debug)
      run: ./gradlew assembleOtherDebug --stacktrace
      
    - name: Build APK (Other Release)
      run: ./gradlew assembleOtherRelease --stacktrace
      continue-on-error: true
      
    - name: List built APKs
      run: find app/build/outputs/apk -name "*.apk" -type f
      
    - name: Upload APK artifacts
      uses: actions/upload-artifact@v4
      with:
        name: sing-box-apks
        path: app/build/outputs/apk/**/*.apk
        retention-days: 30
